#set page(width: auto, height: auto, margin: 1em)
#set text(size: 16pt)

= Hello from Typst

This is a *Typst* document rendered as _SVG_.

#table(
  columns: (auto, auto, auto),
  [*Name*], [*Age*], [*City*],
  [Alice], [30], [New York],
  [Bob], [25], [London],
  [Carol], [35], [Tokyo],
)

The quadratic formula:

$ x = (-b plus.minus sqrt(b^2 - 4 a c)) / (2a) $
